\# Assignment A06 - Graph Plot



\## cloning my GitHub repository:

$ git clone git@github.com:ValerieBizova/ci2.git
$ cd ci2



\## installing jupyter, notebook and panda:

$ python -m pip install jupyter

$ python -m pip install notebook

$ python -m pip install pandas matplotlib



\## opening notebook in browser:

$ python -m notebook



\## running the script in git bash:

$ python graph.py



\## pushing A06 folder into my GitHub repository:

$ git add -A

$ git commit -m "Add assignment A06"

$ git push

